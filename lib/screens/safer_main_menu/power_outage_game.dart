import 'package:flutter/material.dart';

class PowerOutageGame extends StatelessWidget {
  const PowerOutageGame({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Power Outage Game')),
      body: const Center(
        child: Text('Game coming soon…'),
      ),
    );
  }
}
